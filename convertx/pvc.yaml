apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: convertx-data
  namespace: app-dev
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
